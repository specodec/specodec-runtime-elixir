# Generated by generate_emit_runner.mjs. DO NOT EDIT.
defmodule EmitRunner do
alias Specodec.Generated.{
  OptInner,
  SingleString,
  SingleBoolean,
  SingleInt8,
  SingleInt16,
  SingleInt32,
  SingleInt64,
  SingleUint8,
  SingleUint16,
  SingleUint32,
  SingleUint64,
  SingleFloat32,
  SingleFloat64,
  SingleBytes,
  OptSingleString,
  OptSingleBoolean,
  OptSingleInt8,
  OptSingleInt16,
  OptSingleInt32,
  OptSingleInt64,
  OptSingleUint8,
  OptSingleUint16,
  OptSingleUint32,
  OptSingleUint64,
  OptSingleFloat32,
  OptSingleFloat64,
  OptSingleBytes,
  PairString,
  PairBoolean,
  PairInt8,
  PairInt16,
  PairInt32,
  PairInt64,
  PairUint8,
  PairUint16,
  PairUint32,
  PairUint64,
  PairFloat32,
  PairFloat64,
  PairBytes,
  DualStringInt32,
  DualStringBoolean,
  DualStringFloat64,
  DualStringBytes,
  DualInt32Boolean,
  DualInt32Float64,
  DualInt32Int64,
  DualInt32Uint32,
  DualInt64Uint64,
  DualFloat32Float64,
  DualFloat64Boolean,
  DualFloat64Bytes,
  DualUint32Uint64,
  DualBooleanBytes,
  DualInt8Uint8,
  DualInt16Uint16,
  DualStringInt64,
  DualStringUint64,
  DualInt32Bytes,
  DualFloat64Int32,
  DualBooleanInt32,
  DualBytesInt64,
  DualInt8Float32,
  DualUint8Int16,
  DualInt64Float64,
  DualUint64String,
  Triple01,
  Triple02,
  Triple03,
  Triple04,
  Triple05,
  Triple06,
  Triple07,
  Triple08,
  Triple09,
  Triple10,
  Triple11,
  Triple12,
  Triple13,
  Triple14,
  Triple15,
  Five01,
  Five02,
  Five03,
  Five04,
  Five05,
  Five06,
  Five07,
  Five08,
  Five09,
  Five10,
  Ten01,
  Ten02,
  Ten03,
  Ten04,
  Ten05,
  ArrString,
  ArrInt32,
  ArrBoolean,
  ArrFloat64,
  ArrBytes,
  ArrInt64,
  ArrUint64,
  MultiArr1,
  MultiArr2,
  MultiArr3,
  MultiArr4,
  MultiArr5,
  OptCombo1,
  OptCombo2,
  OptCombo3,
  OptCombo4,
  OptCombo5,
  OptCombo6,
  OptCombo7,
  OptCombo8,
  OptCombo9,
  OptCombo10,
  NestInner,
  NestCoord,
  NestIdVal,
  NestLabel,
  NestMoney,
  NestRange32,
  NestAddr,
  NestPoint3,
  OptNestInner,
  OptNestCoord,
  OptNestIdVal,
  OptNestLabel,
  OptNestMoney,
  OptNestRange32,
  OptNestAddr,
  OptNestPoint3,
  ModelArr1,
  ModelArr2,
  ModelArr3,
  ModelArr4,
  ModelArr5,
  Mix01,
  Mix02,
  Mix03,
  Mix04,
  Mix05,
  Mix06,
  Mix07,
  Mix08,
  Mix09,
  Mix10,
  Mix11,
  Mix12,
  Mix13,
  Mix14,
  Mix15,
  AllOpt1,
  AllOpt2,
  AllOpt3,
  AllOpt4,
  AllOpt5,
  RecList,
  RecTree,
  RecChain,
  RecWrap,
  RecWide,
  Wide20,
  Wide25,
  Wide30,
  Wide35,
  Wide40,
  EdgeEmpty,
  EdgeOneOpt,
  EdgeBigNums,
  EdgeZeroVals,
  EdgeNullable,
  EdgeNegZero,
  EdgeNullByte,
  EdgeBoundary,
  EdgeStrLen,
  EdgeBytesLen,
  EdgeArrEmpty,
  EdgeArrBoundary,
  OptArr1,
  OptArr2,
  OptArr3,
  OptArr4,
  OptArr5,
  NestOpt1,
  NestOpt2,
  NestOpt3,
  NestOpt4,
  NestOpt5,
  NestOptInner1,
  NestOptInner2,
  NestOptInner3,
  DeepNest1,
  DeepNest2,
  DeepNest3,
  DeepNest4,
  DeepNest5,
  DeepNest6,
  DeepNest7,
  TimestampEntry,
  ConfigEntry,
  NestedSimple,
  DeepModel,
  EnumHolder,
  OptEnumHolder,
  EnumArrayHolder,
  EnumMixedHolder,
  UnionFieldHolder,
  OptUnionFieldHolder,
  UnionArrayHolder,
  UnionMixedHolder,
  UnionScalarHolder,
  Shape,
  Shape,
  Ident,
  Ident,
  ResultMsg,
  ResultMsg,
  Tagged,
  Tagged,
  Tagged,
  OptUnionHolder,
  OptUnionHolder,
  MixedUnion,
  MixedUnion,
  MixedUnion,
  NestedUnion,
  NestedUnion,
  ScalarUnion,
  ScalarUnion,
  ScalarUnion,
  ScalarUnion
}
alias Specodec.{MsgPackReader, MsgPackWriter, JsonReader, JsonWriter, GronReader, GronWriter}


def test_int8_min(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int8_min.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int8_min.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int8_min: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int8_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int8_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int8_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int8_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int16_min(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int16_min.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int16_min.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int16_min: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int16_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int16_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int16_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int16_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int32_min(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int32_min.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int32_min.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int32_min: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int32_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int32_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int32_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int32_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int64_min(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int64_min.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int64_min.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int64_min: #{Exception.message(e)}"); {0, 1}
  end
end

def test_int64_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/int64_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_int64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_int64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/int64_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL int64_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_uint8_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/uint8_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_uint32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_uint32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/uint8_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL uint8_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_uint16_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/uint16_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_uint32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_uint32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/uint16_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL uint16_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_uint32_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/uint32_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_uint32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_uint32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/uint32_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL uint32_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_uint64_max(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/uint64_max.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_uint64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_uint64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/uint64_max.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL uint64_max: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float32_1_5(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float32_1.5.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float32_1.5.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float32_1.5: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float32_neg_zero(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float32_neg_zero.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float32_neg_zero.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float32_neg_zero: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float32_inf(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float32_inf.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float32_inf.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float32_inf: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float32_neg_inf(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float32_neg_inf.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float32_neg_inf.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float32_neg_inf: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float32_nan(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float32_nan.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float32(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float32(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float32_nan.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float32_nan: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float64_pi(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float64_pi.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float64_pi.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float64_pi: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float64_neg_zero(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float64_neg_zero.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float64_neg_zero.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float64_neg_zero: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float64_inf(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float64_inf.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float64_inf.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float64_inf: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float64_neg_inf(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float64_neg_inf.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float64_neg_inf.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float64_neg_inf: #{Exception.message(e)}"); {0, 1}
  end
end

def test_float64_nan(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/float64_nan.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_float64(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_float64(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/float64_nan.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL float64_nan: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_empty(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_empty.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_empty.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_empty: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_ascii(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_ascii.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_ascii.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_ascii: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_null_byte(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_null_byte.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_null_byte.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_null_byte: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_escape(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_escape.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_escape.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_escape: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_unicode(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_unicode.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_unicode.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_unicode: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_31(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_31.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_31.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_31: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_32(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_32.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_32.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_32: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_255(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_255.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_255.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_255: #{Exception.message(e)}"); {0, 1}
  end
end

def test_str_256(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/str_256.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_string(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_string(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/str_256.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL str_256: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_empty(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_empty.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_empty.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_empty: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_small(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_small.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_small.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_small: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_31(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_31.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_31.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_31: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_32(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_32.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_32.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_32: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_255(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_255.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_255.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_255: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_256(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_256.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_256.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_256: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_zeros(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_zeros.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_zeros.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_zeros: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bytes_ff(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bytes_ff.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bytes(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bytes(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bytes_ff.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bytes_ff: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bool_true(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bool_true.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bool(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bool(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bool_true.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bool_true: #{Exception.message(e)}"); {0, 1}
  end
end

def test_bool_false(vec, out) do
  try do
    expected = File.read!(Path.join(vec, "scalars/bool_false.mp"))
    reader = MsgPackReader.new(expected)
    {val, _r} = MsgPackReader.read_bool(reader)
    writer = MsgPackWriter.new()
    w = MsgPackWriter.write_bool(writer, val)
    actual = MsgPackWriter.to_bytes(w)
    File.mkdir_p!(Path.join(out, "scalars"))
    File.write!(Path.join(out, "scalars/bool_false.mp"), actual)
    if actual == expected, do: {1, 0}, else: {0, 1}
  rescue
    e -> IO.puts(:stderr, "FAIL bool_false: #{Exception.message(e)}"); {0, 1}
  end
end

def test_opt_inner(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptInner.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptInner.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptInner.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptInner mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptInner mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptInner.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptInner.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptInner.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptInner json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptInner json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptInner.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptInner.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptInner.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptInner unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptInner unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptInner.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptInner.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptInner.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptInner gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptInner gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_string(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleString.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleString.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleString.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleString mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleString mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleString.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleString.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleString.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleString json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleString json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleString.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleString.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleString.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleString unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleString unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleString.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleString.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleString.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleString gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleString gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_boolean(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleBoolean.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleBoolean.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleBoolean.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleBoolean.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleBoolean.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleBoolean.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleBoolean.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleBoolean.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleBoolean.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleBoolean.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleBoolean.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleBoolean.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBoolean gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_int8(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleInt8.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleInt8.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleInt8.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt8.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt8.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleInt8.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleInt8.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleInt8.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt8 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_int16(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleInt16.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleInt16.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleInt16.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt16.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt16.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleInt16.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleInt16.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleInt16.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt16 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_int32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleInt32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleInt32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleInt32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleInt32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleInt32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleInt32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_int64(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleInt64.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleInt64.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleInt64.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt64.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleInt64.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleInt64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleInt64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleInt64.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleInt64.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleInt64.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleInt64 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_uint8(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleUint8.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleUint8.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleUint8.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint8.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint8.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleUint8.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleUint8.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleUint8.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint8 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_uint16(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleUint16.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleUint16.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleUint16.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint16.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint16.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleUint16.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleUint16.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleUint16.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint16 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_uint32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleUint32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleUint32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleUint32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleUint32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleUint32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleUint32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_uint64(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleUint64.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleUint64.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleUint64.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint64.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleUint64.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleUint64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleUint64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleUint64.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleUint64.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleUint64.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleUint64 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_float32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleFloat32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleFloat32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleFloat32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleFloat32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleFloat32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleFloat32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleFloat32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleFloat32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleFloat32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleFloat32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleFloat32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleFloat32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_float64(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleFloat64.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleFloat64.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleFloat64.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleFloat64.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleFloat64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleFloat64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleFloat64.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleFloat64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleFloat64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleFloat64.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleFloat64.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleFloat64.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleFloat64 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_single_bytes(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "SingleBytes.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = SingleBytes.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = SingleBytes.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "SingleBytes.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleBytes.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleBytes.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "SingleBytes.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = SingleBytes.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = SingleBytes.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "SingleBytes.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = SingleBytes.decode(GronReader, reader)
    writer = GronWriter.new()
    w = SingleBytes.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL SingleBytes gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_string(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleString.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleString.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleString.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleString.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleString.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleString.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleString.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleString.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleString.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleString.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleString.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleString.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleString gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_boolean(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleBoolean.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleBoolean.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleBoolean.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleBoolean.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleBoolean.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleBoolean.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleBoolean.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleBoolean.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleBoolean.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleBoolean.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleBoolean.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleBoolean.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBoolean gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_int8(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt8.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleInt8.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleInt8.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt8.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt8.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt8.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleInt8.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleInt8.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt8 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_int16(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt16.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleInt16.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleInt16.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt16.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt16.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt16.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleInt16.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleInt16.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt16 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_int32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleInt32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleInt32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleInt32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleInt32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_int64(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt64.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleInt64.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleInt64.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt64.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt64.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleInt64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleInt64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleInt64.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleInt64.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleInt64.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleInt64 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_uint8(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint8.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleUint8.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleUint8.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint8.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint8.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint8.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleUint8.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleUint8.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint8 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_uint16(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint16.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleUint16.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleUint16.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint16.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint16.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint16.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint16.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint16.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleUint16.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleUint16.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint16 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_uint32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleUint32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleUint32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleUint32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleUint32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_uint64(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint64.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleUint64.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleUint64.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint64.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint64.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleUint64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleUint64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleUint64.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleUint64.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleUint64.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleUint64 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_float32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleFloat32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleFloat32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleFloat32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleFloat32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleFloat32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleFloat32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleFloat32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleFloat32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_float64(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat64.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleFloat64.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleFloat64.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat64.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleFloat64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleFloat64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat64.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleFloat64.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleFloat64.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleFloat64.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleFloat64.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleFloat64.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleFloat64 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_single_bytes(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptSingleBytes.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptSingleBytes.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptSingleBytes.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleBytes.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleBytes.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleBytes.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptSingleBytes.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptSingleBytes.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptSingleBytes.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptSingleBytes.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptSingleBytes.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptSingleBytes.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptSingleBytes gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_pair_string(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "PairString.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = PairString.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = PairString.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairString mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairString mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "PairString.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = PairString.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = PairString.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairString json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairString json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "PairString.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = PairString.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = PairString.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairString unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairString unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "PairString.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = PairString.decode(GronReader, reader)
    writer = GronWriter.new()
    w = PairString.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairString gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairString gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_pair_boolean(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "PairBoolean.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = PairBoolean.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = PairBoolean.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "PairBoolean.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = PairBoolean.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = PairBoolean.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "PairBoolean.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = PairBoolean.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = PairBoolean.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "PairBoolean.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = PairBoolean.decode(GronReader, reader)
    writer = GronWriter.new()
    w = PairBoolean.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairBoolean gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_pair_int8(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "PairInt8.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = PairInt8.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = PairInt8.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "PairInt8.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = PairInt8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = PairInt8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "PairInt8.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = PairInt8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = PairInt8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "PairInt8.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = PairInt8.decode(GronReader, reader)
    writer = GronWriter.new()
    w = PairInt8.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL PairInt8 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_pair_int16(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "PairInt16.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = PairInt16.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = PairInt16.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL P
encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptCombo8.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptCombo8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptCombo8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptCombo8.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptCombo8.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptCombo8.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptCombo8.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptCombo8.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptCombo8.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo8 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_combo9(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptCombo9.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptCombo9.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptCombo9.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptCombo9.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptCombo9.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptCombo9.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptCombo9.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptCombo9.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptCombo9.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptCombo9.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptCombo9.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptCombo9.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo9 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_combo10(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptCombo10.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptCombo10.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptCombo10.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptCombo10.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptCombo10.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptCombo10.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptCombo10.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptCombo10.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptCombo10.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptCombo10.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptCombo10.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptCombo10.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptCombo10 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_inner(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestInner.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestInner.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestInner.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestInner mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestInner mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestInner.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestInner.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestInner.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestInner json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestInner json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestInner.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestInner.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestInner.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestInner unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestInner unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestInner.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestInner.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestInner.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestInner gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestInner gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_coord(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestCoord.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestCoord.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestCoord.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestCoord.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestCoord.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestCoord.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestCoord.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestCoord.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestCoord.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestCoord.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestCoord.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestCoord.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestCoord gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_id_val(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestIdVal.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestIdVal.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestIdVal.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestIdVal.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestIdVal.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestIdVal.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestIdVal.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestIdVal.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestIdVal.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestIdVal.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestIdVal.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestIdVal.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestIdVal gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_label(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestLabel.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestLabel.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestLabel.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestLabel.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestLabel.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestLabel.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestLabel.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestLabel.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestLabel.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestLabel.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestLabel.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestLabel.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestLabel gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_money(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestMoney.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestMoney.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestMoney.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestMoney.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestMoney.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestMoney.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestMoney.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestMoney.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestMoney.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestMoney.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestMoney.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestMoney.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestMoney gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_range32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestRange32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestRange32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestRange32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestRange32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestRange32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestRange32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestRange32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestRange32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestRange32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestRange32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestRange32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestRange32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestRange32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_addr(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestAddr.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestAddr.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestAddr.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestAddr.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestAddr.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestAddr.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestAddr.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestAddr.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestAddr.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestAddr.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestAddr.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestAddr.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestAddr gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_point3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestPoint3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestPoint3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestPoint3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestPoint3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestPoint3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestPoint3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestPoint3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestPoint3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestPoint3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestPoint3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestPoint3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestPoint3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestPoint3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_inner(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestInner.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestInner.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestInner.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestInner.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestInner.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestInner.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestInner.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestInner.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestInner.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestInner.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestInner.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestInner.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestInner gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_coord(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestCoord.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestCoord.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestCoord.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestCoord.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestCoord.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestCoord.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestCoord.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestCoord.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestCoord.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestCoord.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestCoord.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestCoord.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestCoord gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_id_val(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestIdVal.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestIdVal.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestIdVal.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestIdVal.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestIdVal.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestIdVal.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestIdVal.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestIdVal.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestIdVal.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestIdVal.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestIdVal.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestIdVal.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestIdVal gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_label(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestLabel.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestLabel.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestLabel.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestLabel.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestLabel.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestLabel.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestLabel.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestLabel.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestLabel.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestLabel.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestLabel.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestLabel.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestLabel gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_money(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestMoney.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestMoney.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestMoney.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestMoney.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestMoney.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestMoney.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestMoney.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestMoney.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestMoney.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestMoney.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestMoney.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestMoney.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestMoney gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_range32(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestRange32.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestRange32.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestRange32.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestRange32.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestRange32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestRange32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestRange32.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestRange32.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestRange32.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestRange32.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestRange32.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestRange32.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestRange32 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_addr(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestAddr.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestAddr.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestAddr.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestAddr.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestAddr.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestAddr.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestAddr.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestAddr.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestAddr.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestAddr.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestAddr.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestAddr.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestAddr gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_nest_point3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptNestPoint3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptNestPoint3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptNestPoint3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestPoint3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestPoint3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestPoint3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptNestPoint3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptNestPoint3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptNestPoint3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptNestPoint3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptNestPoint3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptNestPoint3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptNestPoint3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_model_arr1(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ModelArr1.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ModelArr1.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ModelArr1.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr1.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr1.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ModelArr1.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ModelArr1.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ModelArr1.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr1 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_model_arr2(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ModelArr2.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ModelArr2.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ModelArr2.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr2.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr2.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ModelArr2.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ModelArr2.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ModelArr2.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr2 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_model_arr3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ModelArr3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ModelArr3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ModelArr3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ModelArr3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ModelArr3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ModelArr3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_model_arr4(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ModelArr4.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ModelArr4.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ModelArr4.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr4.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr4.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ModelArr4.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ModelArr4.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ModelArr4.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr4 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_model_arr5(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ModelArr5.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ModelArr5.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ModelArr5.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr5.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ModelArr5.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ModelArr5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ModelArr5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ModelArr5.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ModelArr5.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ModelArr5.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ModelArr5 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix01(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix01.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix01.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix01.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix01.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix01.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix01.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix01.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix01.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix01.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix01.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix01.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix01.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix01 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix02(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix02.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix02.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix02.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix02.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix02.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix02.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix02.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix02.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix02.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix02.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix02.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix02.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix02 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix03(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix03.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix03.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix03.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix03.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix03.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix03.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix03.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix03.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix03.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix03.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix03.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix03.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix03 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix04(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix04.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix04.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix04.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix04.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix04.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix04.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix04.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix04.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix04.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix04.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix04.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix04.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix04 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix05(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix05.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix05.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix05.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix05.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix05.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix05.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix05.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix05.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix05.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix05.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix05.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix05.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix05 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix06(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix06.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix06.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix06.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix06.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix06.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix06.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix06.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix06.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix06.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix06.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix06.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix06.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix06 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix07(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix07.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix07.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix07.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix07.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix07.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix07.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix07.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix07.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix07.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix07.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix07.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix07.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix07 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix08(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix08.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix08.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix08.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix08.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix08.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix08.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix08.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix08.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix08.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix08.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix08.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix08.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix08 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix09(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix09.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix09.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix09.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix09.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix09.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix09.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix09.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix09.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix09.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix09.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix09.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix09.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix09 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix10(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix10.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix10.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix10.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix10.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix10.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix10.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix10.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix10.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix10.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix10.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix10.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix10.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix10 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix11(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix11.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix11.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix11.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix11.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix11.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix11.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix11.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix11.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix11.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix11.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix11.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix11.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix11 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix12(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix12.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix12.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix12.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix12.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix12.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix12.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix12.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix12.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix12.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix12.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix12.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix12.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix12 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix13(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix13.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix13.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix13.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix13.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix13.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix13.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix13.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix13.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix13.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix13.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix13.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix13.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix13 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix14(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix14.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix14.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix14.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix14.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix14.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix14.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix14.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix14.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix14.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix14.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix14.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix14.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix14 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mix15(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Mix15.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Mix15.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Mix15.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Mix15.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix15.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix15.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Mix15.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Mix15.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Mix15.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Mix15.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Mix15.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Mix15.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Mix15 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_all_opt1(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "AllOpt1.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = AllOpt1.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = AllOpt1.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt1.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt1.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "AllOpt1.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = AllOpt1.decode(GronReader, reader)
    writer = GronWriter.new()
    w = AllOpt1.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt1 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_all_opt2(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "AllOpt2.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = AllOpt2.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = AllOpt2.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt2.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt2.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "AllOpt2.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = AllOpt2.decode(GronReader, reader)
    writer = GronWriter.new()
    w = AllOpt2.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt2 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_all_opt3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "AllOpt3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = AllOpt3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = AllOpt3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "AllOpt3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = AllOpt3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = AllOpt3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_all_opt4(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "AllOpt4.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = AllOpt4.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = AllOpt4.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt4.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt4.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "AllOpt4.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = AllOpt4.decode(GronReader, reader)
    writer = GronWriter.new()
    w = AllOpt4.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt4 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_all_opt5(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "AllOpt5.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = AllOpt5.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = AllOpt5.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt5.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "AllOpt5.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = AllOpt5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = AllOpt5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "AllOpt5.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = AllOpt5.decode(GronReader, reader)
    writer = GronWriter.new()
    w = AllOpt5.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL AllOpt5 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_rec_list(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "RecList.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = RecList.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = RecList.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecList mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecList mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "RecList.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecList.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecList.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecList json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecList json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "RecList.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecList.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecList.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecList unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecList unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "RecList.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = RecList.decode(GronReader, reader)
    writer = GronWriter.new()
    w = RecList.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecList gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecList gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_rec_tree(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "RecTree.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = RecTree.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = RecTree.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecTree mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecTree mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "RecTree.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecTree.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecTree.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecTree json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecTree json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "RecTree.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecTree.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecTree.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecTree unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecTree unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "RecTree.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = RecTree.decode(GronReader, reader)
    writer = GronWriter.new()
    w = RecTree.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecTree gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecTree gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_rec_chain(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "RecChain.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = RecChain.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = RecChain.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecChain mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecChain mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "RecChain.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecChain.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecChain.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecChain json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecChain json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "RecChain.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecChain.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecChain.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecChain unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecChain unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "RecChain.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = RecChain.decode(GronReader, reader)
    writer = GronWriter.new()
    w = RecChain.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecChain gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecChain gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_rec_wrap(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "RecWrap.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = RecWrap.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = RecWrap.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "RecWrap.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecWrap.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecWrap.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "RecWrap.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecWrap.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecWrap.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "RecWrap.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = RecWrap.decode(GronReader, reader)
    writer = GronWriter.new()
    w = RecWrap.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWrap gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_rec_wide(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "RecWide.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = RecWide.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = RecWide.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWide mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWide mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "RecWide.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecWide.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecWide.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWide json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWide json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "RecWide.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = RecWide.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = RecWide.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWide unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWide unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "RecWide.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = RecWide.decode(GronReader, reader)
    writer = GronWriter.new()
    w = RecWide.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL RecWide gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL RecWide gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_wide20(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Wide20.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Wide20.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Wide20.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Wide20.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide20.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide20.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Wide20.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide20.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide20.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Wide20.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Wide20.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Wide20.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide20 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_wide25(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Wide25.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Wide25.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Wide25.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Wide25.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide25.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide25.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Wide25.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide25.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide25.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Wide25.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Wide25.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Wide25.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide25 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_wide30(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Wide30.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Wide30.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Wide30.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Wide30.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide30.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide30.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Wide30.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide30.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide30.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Wide30.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Wide30.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Wide30.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide30 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_wide35(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Wide35.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Wide35.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Wide35.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Wide35.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide35.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide35.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Wide35.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide35.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide35.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Wide35.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Wide35.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Wide35.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide35 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_wide40(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Wide40.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Wide40.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Wide40.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Wide40.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide40.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide40.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Wide40.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Wide40.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Wide40.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Wide40.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Wide40.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Wide40.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Wide40 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_empty(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeEmpty.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeEmpty.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeEmpty.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeEmpty.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeEmpty.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeEmpty.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeEmpty.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeEmpty.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeEmpty.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeEmpty.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeEmpty.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeEmpty.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeEmpty gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_one_opt(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeOneOpt.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeOneOpt.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeOneOpt.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeOneOpt.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeOneOpt.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeOneOpt.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeOneOpt.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeOneOpt.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeOneOpt.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeOneOpt.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeOneOpt.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeOneOpt.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeOneOpt gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_big_nums(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeBigNums.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeBigNums.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeBigNums.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeBigNums.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeBigNums.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeBigNums.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeBigNums.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeBigNums.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeBigNums.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeBigNums.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeBigNums.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeBigNums.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBigNums gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_zero_vals(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeZeroVals.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeZeroVals.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeZeroVals.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeZeroVals.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeZeroVals.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeZeroVals.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeZeroVals.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeZeroVals.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeZeroVals.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeZeroVals.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeZeroVals.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeZeroVals.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeZeroVals gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_nullable(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeNullable.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeNullable.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeNullable.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeNullable.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeNullable.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeNullable.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeNullable.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeNullable.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeNullable.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeNullable.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeNullable.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeNullable.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullable gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_neg_zero(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeNegZero.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeNegZero.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeNegZero.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeNegZero.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeNegZero.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeNegZero.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeNegZero.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeNegZero.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeNegZero.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeNegZero.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeNegZero.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeNegZero.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNegZero gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_null_byte(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeNullByte.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeNullByte.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeNullByte.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeNullByte.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeNullByte.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeNullByte.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeNullByte.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeNullByte.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeNullByte.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeNullByte.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeNullByte.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeNullByte.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeNullByte gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_boundary(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeBoundary.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeBoundary.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeBoundary.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeBoundary.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeBoundary.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeBoundary.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeBoundary.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeBoundary.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeBoundary.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeBoundary.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeBoundary.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeBoundary.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBoundary gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_str_len(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeStrLen.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeStrLen.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeStrLen.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeStrLen.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeStrLen.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeStrLen.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeStrLen.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeStrLen.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeStrLen.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeStrLen.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeStrLen.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeStrLen.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeStrLen gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_bytes_len(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeBytesLen.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeBytesLen.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeBytesLen.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeBytesLen.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeBytesLen.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeBytesLen.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeBytesLen.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeBytesLen.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeBytesLen.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeBytesLen.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeBytesLen.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeBytesLen.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeBytesLen gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_arr_empty(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeArrEmpty.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeArrEmpty.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeArrEmpty.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeArrEmpty.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeArrEmpty.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeArrEmpty.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeArrEmpty.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeArrEmpty.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeArrEmpty.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeArrEmpty.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeArrEmpty.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeArrEmpty.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrEmpty gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_edge_arr_boundary(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EdgeArrBoundary.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EdgeArrBoundary.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EdgeArrBoundary.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeArrBoundary.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeArrBoundary.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeArrBoundary.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EdgeArrBoundary.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EdgeArrBoundary.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EdgeArrBoundary.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EdgeArrBoundary.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EdgeArrBoundary.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EdgeArrBoundary.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EdgeArrBoundary gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_arr1(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptArr1.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptArr1.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptArr1.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr1.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr1.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptArr1.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptArr1.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptArr1.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr1 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_arr2(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptArr2.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptArr2.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptArr2.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr2.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr2.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptArr2.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptArr2.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptArr2.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr2 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_arr3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptArr3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptArr3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptArr3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptArr3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptArr3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptArr3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_arr4(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptArr4.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptArr4.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptArr4.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr4.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr4.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptArr4.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptArr4.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptArr4.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr4 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_arr5(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptArr5.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptArr5.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptArr5.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr5.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptArr5.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptArr5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptArr5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptArr5.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptArr5.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptArr5.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptArr5 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt1(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOpt1.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOpt1.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOpt1.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt1.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt1.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOpt1.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOpt1.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOpt1.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt1 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt2(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOpt2.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOpt2.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOpt2.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt2.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt2.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOpt2.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOpt2.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOpt2.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt2 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOpt3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOpt3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOpt3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOpt3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOpt3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOpt3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt4(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOpt4.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOpt4.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOpt4.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt4.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt4.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOpt4.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOpt4.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOpt4.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt4 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt5(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOpt5.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOpt5.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOpt5.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt5.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOpt5.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOpt5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOpt5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOpt5.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOpt5.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOpt5.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOpt5 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt_inner1(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOptInner1.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOptInner1.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOptInner1.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOptInner1.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOptInner1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOptInner1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOptInner1.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOptInner1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOptInner1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOptInner1.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOptInner1.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOptInner1.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner1 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt_inner2(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOptInner2.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOptInner2.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOptInner2.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOptInner2.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOptInner2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOptInner2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOptInner2.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOptInner2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOptInner2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOptInner2.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOptInner2.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOptInner2.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner2 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nest_opt_inner3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestOptInner3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestOptInner3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestOptInner3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestOptInner3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOptInner3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOptInner3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestOptInner3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestOptInner3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestOptInner3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestOptInner3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestOptInner3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestOptInner3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestOptInner3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest1(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest1.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest1.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest1.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest1.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest1.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest1.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest1.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest1.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest1.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest1.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest1 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest2(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest2.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest2.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest2.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest2.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest2.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest2.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest2.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest2.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest2.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest2.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest2 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest3(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest3.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest3.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest3.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest3.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest3.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest3.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest3.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest3.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest3.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest3.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest3 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest4(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest4.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest4.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest4.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest4.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest4.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest4.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest4.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest4.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest4.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest4.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest4 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest5(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest5.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest5.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest5.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest5.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest5.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest5.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest5.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest5.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest5.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest5.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest5 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest6(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest6.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest6.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest6.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest6.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest6.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest6.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest6.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest6.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest6.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest6.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest6.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest6.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest6 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_nest7(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepNest7.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepNest7.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepNest7.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest7.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest7.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest7.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepNest7.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepNest7.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepNest7.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepNest7.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepNest7.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepNest7.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepNest7 gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_timestamp_entry(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "TimestampEntry.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = TimestampEntry.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = TimestampEntry.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "TimestampEntry.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = TimestampEntry.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = TimestampEntry.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "TimestampEntry.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = TimestampEntry.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = TimestampEntry.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "TimestampEntry.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = TimestampEntry.decode(GronReader, reader)
    writer = GronWriter.new()
    w = TimestampEntry.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL TimestampEntry gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_config_entry(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ConfigEntry.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ConfigEntry.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ConfigEntry.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ConfigEntry.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ConfigEntry.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ConfigEntry.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ConfigEntry.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ConfigEntry.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ConfigEntry.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ConfigEntry.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ConfigEntry.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ConfigEntry.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ConfigEntry gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nested_simple(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestedSimple.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestedSimple.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestedSimple.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestedSimple.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestedSimple.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestedSimple.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestedSimple.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestedSimple.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestedSimple.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestedSimple.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestedSimple.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestedSimple.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedSimple gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_deep_model(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "DeepModel.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = DeepModel.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = DeepModel.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "DeepModel.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepModel.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepModel.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "DeepModel.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = DeepModel.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = DeepModel.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "DeepModel.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = DeepModel.decode(GronReader, reader)
    writer = GronWriter.new()
    w = DeepModel.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL DeepModel gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_enum_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EnumHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EnumHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EnumHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EnumHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EnumHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EnumHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EnumHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EnumHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EnumHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EnumHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EnumHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EnumHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_enum_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptEnumHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptEnumHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptEnumHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptEnumHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptEnumHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptEnumHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptEnumHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptEnumHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptEnumHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptEnumHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptEnumHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptEnumHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptEnumHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_enum_array_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EnumArrayHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EnumArrayHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EnumArrayHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EnumArrayHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EnumArrayHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EnumArrayHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EnumArrayHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EnumArrayHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EnumArrayHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EnumArrayHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EnumArrayHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EnumArrayHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumArrayHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_enum_mixed_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "EnumMixedHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = EnumMixedHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = EnumMixedHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "EnumMixedHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EnumMixedHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EnumMixedHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "EnumMixedHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = EnumMixedHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = EnumMixedHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "EnumMixedHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = EnumMixedHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = EnumMixedHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL EnumMixedHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_union_field_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "UnionFieldHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = UnionFieldHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = UnionFieldHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "UnionFieldHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionFieldHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionFieldHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "UnionFieldHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionFieldHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionFieldHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "UnionFieldHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = UnionFieldHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = UnionFieldHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionFieldHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_union_field_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptUnionFieldHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptUnionFieldHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptUnionFieldHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptUnionFieldHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptUnionFieldHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptUnionFieldHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptUnionFieldHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptUnionFieldHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptUnionFieldHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptUnionFieldHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptUnionFieldHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptUnionFieldHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionFieldHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_union_array_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "UnionArrayHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = UnionArrayHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = UnionArrayHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "UnionArrayHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionArrayHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionArrayHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "UnionArrayHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionArrayHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionArrayHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "UnionArrayHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = UnionArrayHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = UnionArrayHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionArrayHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_union_mixed_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "UnionMixedHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = UnionMixedHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = UnionMixedHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "UnionMixedHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionMixedHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionMixedHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "UnionMixedHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionMixedHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionMixedHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "UnionMixedHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = UnionMixedHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = UnionMixedHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionMixedHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_union_scalar_holder(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "UnionScalarHolder.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = UnionScalarHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = UnionScalarHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "UnionScalarHolder.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionScalarHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionScalarHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "UnionScalarHolder.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = UnionScalarHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = UnionScalarHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "UnionScalarHolder.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = UnionScalarHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = UnionScalarHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL UnionScalarHolder gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_shape_circle(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Shape_circle.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Shape.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Shape.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Shape_circle.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Shape.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Shape.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Shape_circle.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Shape.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Shape.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Shape_circle.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Shape.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Shape.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_circle gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_shape_rect(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Shape_rect.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Shape.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Shape.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Shape_rect.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Shape.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Shape.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Shape_rect.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Shape.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Shape.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Shape_rect.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Shape.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Shape.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Shape_rect gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_ident_name(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Ident_name.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Ident.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Ident.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Ident_name.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Ident.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Ident.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Ident_name.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Ident.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Ident.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Ident_name.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Ident.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Ident.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_name gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_ident_number(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Ident_number.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Ident.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Ident.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Ident_number.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Ident.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Ident.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Ident_number.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Ident.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Ident.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Ident_number.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Ident.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Ident.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Ident_number gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_result_msg_ok(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_ok.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ResultMsg.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ResultMsg.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_ok.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ResultMsg.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ResultMsg.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_ok.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ResultMsg.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ResultMsg.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_ok.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ResultMsg.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ResultMsg.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_ok gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_result_msg_err(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_err.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ResultMsg.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ResultMsg.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_err.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ResultMsg.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ResultMsg.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_err.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ResultMsg.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ResultMsg.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ResultMsg_err.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ResultMsg.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ResultMsg.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ResultMsg_err gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_tagged_tag(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Tagged_tag.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Tagged.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Tagged.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Tagged_tag.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Tagged.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Tagged.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Tagged_tag.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Tagged.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Tagged.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Tagged_tag.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Tagged.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Tagged.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_tag gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_tagged_score(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Tagged_score.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Tagged.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Tagged.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Tagged_score.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Tagged.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Tagged.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Tagged_score.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Tagged.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Tagged.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Tagged_score.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Tagged.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Tagged.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_score gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_tagged_active(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "Tagged_active.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = Tagged.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = Tagged.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "Tagged_active.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Tagged.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Tagged.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "Tagged_active.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = Tagged.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = Tagged.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "Tagged_active.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = Tagged.decode(GronReader, reader)
    writer = GronWriter.new()
    w = Tagged.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL Tagged_active gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_union_holder_shape(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_shape.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptUnionHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_shape.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptUnionHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_shape.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptUnionHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_shape.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptUnionHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_shape gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_opt_union_holder_id(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_id.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = OptUnionHolder.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_id.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptUnionHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_id.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = OptUnionHolder.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "OptUnionHolder_id.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = OptUnionHolder.decode(GronReader, reader)
    writer = GronWriter.new()
    w = OptUnionHolder.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL OptUnionHolder_id gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mixed_union_point(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_point.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = MixedUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = MixedUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_point.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = MixedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = MixedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_point.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = MixedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = MixedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_point.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = MixedUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = MixedUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_point gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mixed_union_label(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_label.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = MixedUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = MixedUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_label.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = MixedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = MixedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_label.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = MixedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = MixedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_label.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = MixedUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = MixedUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_label gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_mixed_union_count(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_count.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = MixedUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = MixedUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_count.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = MixedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = MixedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_count.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = MixedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = MixedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "MixedUnion_count.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = MixedUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = MixedUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL MixedUnion_count gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nested_union_result(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_result.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestedUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestedUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_result.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_result.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_result.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestedUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestedUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_result gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_nested_union_shape(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_shape.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = NestedUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = NestedUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_shape.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_shape.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = NestedUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = NestedUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "NestedUnion_shape.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = NestedUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = NestedUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL NestedUnion_shape gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_scalar_union_s(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_s.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ScalarUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ScalarUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_s.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_s.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_s.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ScalarUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ScalarUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_s gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_scalar_union_i(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_i.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ScalarUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ScalarUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_i.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_i.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_i.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ScalarUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ScalarUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_i gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_scalar_union_f(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_f.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ScalarUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ScalarUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_f.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_f.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_f.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ScalarUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ScalarUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_f gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def test_scalar_union_b(vec, out) do
  passed = 0
  failed = 0

  # MsgPack
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_b.msgpack"))
    reader = MsgPackReader.new(expected)
    {obj, _r} = ScalarUnion.decode(MsgPackReader, reader)
    writer = MsgPackWriter.new()
    w = ScalarUnion.encode(MsgPackWriter, writer, obj)
    actual = MsgPackWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b mp"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b mp: #{Exception.message(e)}")
  end

  # JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_b.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b json"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b json: #{Exception.message(e)}")
  end

  # Unformatted JSON
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_b.unformatted.json"))
    reader = JsonReader.new(expected)
    {obj, _r} = ScalarUnion.decode(JsonReader, reader)
    writer = JsonWriter.new()
    w = ScalarUnion.encode(JsonWriter, writer, obj)
    actual = JsonWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b unformatted"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b unformatted: #{Exception.message(e)}")
  end

  # Gron
  try do
    expected = File.read!(Path.join(vec, "ScalarUnion_b.gron"))
    reader = GronReader.new(expected)
    {obj, _r} = ScalarUnion.decode(GronReader, reader)
    writer = GronWriter.new()
    w = ScalarUnion.encode(GronWriter, writer, obj)
    actual = GronWriter.to_bytes(w)
    if actual == expected, do: passed = passed + 1, else: (failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b gron"))
  rescue
    e -> failed = failed + 1; IO.puts(:stderr, "FAIL ScalarUnion_b gron: #{Exception.message(e)}")
  end

  {passed, failed}
end
def run_all(vec, out) do
  File.mkdir_p!(out)
  passed = 0
  failed = 0
  {p, f} = test_int8_min(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int8_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int16_min(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int16_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int32_min(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int32_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int64_min(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_int64_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_uint8_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_uint16_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_uint32_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_uint64_max(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float32_1_5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float32_neg_zero(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float32_inf(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float32_neg_inf(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float32_nan(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float64_pi(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float64_neg_zero(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float64_inf(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float64_neg_inf(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_float64_nan(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_empty(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_ascii(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_null_byte(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_escape(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_unicode(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_31(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_255(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_str_256(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_empty(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_small(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_31(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_255(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_256(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_zeros(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bytes_ff(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bool_true(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_bool_false(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_inner(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_string(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_int8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_int16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_uint8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_uint16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_uint32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_float32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_single_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_string(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_int8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_int16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_uint8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_uint16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_uint32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_float32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_single_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_string(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_int8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_int16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_uint8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_uint16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_uint32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_float32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_pair_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_string_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_string_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_string_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_string_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int32_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int32_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int32_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int32_uint32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int64_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_float32_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_float64_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_float64_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_uint32_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_boolean_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int8_uint8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int16_uint16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_string_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_string_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int32_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_float64_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_boolean_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_bytes_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int8_float32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_uint8_int16(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_int64_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_dual_uint64_string(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple01(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple02(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple03(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple04(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple05(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple06(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple07(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple08(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple09(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple10(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple11(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple12(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple13(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple14(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_triple15(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five01(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five02(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five03(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five04(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five05(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five06(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five07(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five08(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five09(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_five10(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ten01(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ten02(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ten03(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ten04(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ten05(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_string(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_int32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_boolean(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_float64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_bytes(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_int64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_arr_uint64(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_multi_arr1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_multi_arr2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_multi_arr3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_multi_arr4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_multi_arr5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo6(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo7(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo8(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo9(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_combo10(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_inner(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_coord(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_id_val(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_label(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_money(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_range32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_addr(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_point3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_inner(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_coord(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_id_val(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_label(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_money(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_range32(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_addr(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_nest_point3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_model_arr1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_model_arr2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_model_arr3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_model_arr4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_model_arr5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix01(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix02(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix03(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix04(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix05(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix06(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix07(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix08(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix09(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix10(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix11(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix12(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix13(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix14(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mix15(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_all_opt1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_all_opt2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_all_opt3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_all_opt4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_all_opt5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_rec_list(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_rec_tree(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_rec_chain(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_rec_wrap(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_rec_wide(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_wide20(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_wide25(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_wide30(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_wide35(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_wide40(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_empty(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_one_opt(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_big_nums(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_zero_vals(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_nullable(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_neg_zero(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_null_byte(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_boundary(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_str_len(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_bytes_len(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_arr_empty(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_edge_arr_boundary(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_arr1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_arr2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_arr3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_arr4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_arr5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt_inner1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt_inner2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nest_opt_inner3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest1(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest2(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest3(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest4(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest5(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest6(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_nest7(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_timestamp_entry(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_config_entry(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nested_simple(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_deep_model(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_enum_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_enum_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_enum_array_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_enum_mixed_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_union_field_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_union_field_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_union_array_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_union_mixed_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_union_scalar_holder(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_shape_circle(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_shape_rect(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ident_name(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_ident_number(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_result_msg_ok(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_result_msg_err(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_tagged_tag(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_tagged_score(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_tagged_active(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_union_holder_shape(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_opt_union_holder_id(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mixed_union_point(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mixed_union_label(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_mixed_union_count(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nested_union_result(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_nested_union_shape(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_scalar_union_s(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_scalar_union_i(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_scalar_union_f(vec, out); passed = passed + p; failed = failed + f
  {p, f} = test_scalar_union_b(vec, out); passed = passed + p; failed = failed + f

  IO.puts("emit-elixir: #{passed} passed, #{failed} failed")
  if failed > 0, do: System.halt(1)
end

end

vec = System.get_env("VEC_DIR")
out = System.get_env("OUT_DIR")
EmitRunner.run_all(vec, out)
